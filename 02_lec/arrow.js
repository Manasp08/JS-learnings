const user={
    username:'hitesh',
    price:99,

    welcomeMessage:function(){
        console.log(`${this.username},welcome to website`);
    }
}

user.welcomeMessage( )


((name)=>{
    console.log(`db connected ${name}`)
})('manas');//simple iife    for name iife name the function

//execution context

